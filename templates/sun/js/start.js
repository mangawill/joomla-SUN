var Sun = window.Sun || {};

Sun.Table = (funciton($) {
    
    var thisObject = {
        init: function() {
        }
    }
    
    return thisObject;
})(document.id);

window.addEvent("domready", function(){
	Sun.Table.init();
});